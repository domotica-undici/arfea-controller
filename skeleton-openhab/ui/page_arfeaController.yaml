# NB formato: il contenuto della pagina va sotto "slots:" (default/masonry/grid/
# canvas). Le chiavi top-level "blocks:"/"masonry:"/... NON fanno parte del modello
# componente e vengono SCARTATE silenziosamente dalla REST: la pagina si importa
# ma resta vuota (solo config). Vedi /rest/ui/components/ui:page/<uid>.
uid: page_arfeaController
tags: []
props:
  parameters: []
  parameterGroups: []
component: oh-layout-page
config:
  label: Arfea controller
  order: "97"
  sidebar: true
  sidebarIcon: f7:desktopcomputer
slots:
  default:
    - component: oh-block
      config: {}
      slots:
        default:
          - component: oh-grid-row
            config: {}
            slots:
              default:
                - component: oh-grid-col
                  config: {}
                  slots:
                    default:
                      - component: widget:widget_arfea_controller
                        config: {}
  masonry: []
  grid: []
  canvas: []
